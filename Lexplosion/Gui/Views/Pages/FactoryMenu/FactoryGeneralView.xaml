<UserControl
    x:Class="Lexplosion.Gui.Views.Pages.FactoryMenu.FactoryGeneralView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:objects="clr-namespace:Lexplosion"
    d:DesignHeight="450"
    d:DesignWidth="700"
    mc:Ignorable="d">
    <UserControl.Resources>
        <!--  TextBlock  -->
        <Style TargetType="{x:Type TextBlock}">
            <Setter Property="TextAlignment" Value="Center" />
            <Setter Property="FontSize" Value="24" />
            <Setter Property="Foreground" Value="WhiteSmoke" />
            <Setter Property="Padding" Value="5,0,0,0" />
        </Style>
        <!--  Flat ComboBox  -->
        <ControlTemplate x:Key="ComboBoxToggleButton" TargetType="{x:Type ToggleButton}">
            <Border
                x:Name="border"
                Width="{TemplateBinding Width}"
                Height="{TemplateBinding Height}"
                Background="#171717"
                BorderBrush="#121212"
                BorderThickness="1.5"
                CornerRadius="6">
                <Grid
                    Margin="10,0,0,0"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Center">
                    <ContentPresenter />
                </Grid>
            </Border>
            <ControlTemplate.Triggers>
                <Trigger Property="IsChecked" Value="True">
                    <Setter TargetName="border" Property="BorderBrush" Value="#167FFC" />
                    <Setter TargetName="border" Property="BorderThickness" Value="2" />
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>

        <Style x:Key="{x:Type ComboBox}" TargetType="{x:Type ComboBox}">
            <Setter Property="SnapsToDevicePixels" Value="True" />
            <Setter Property="OverridesDefaultStyle" Value="True" />
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto" />
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto" />
            <Setter Property="ScrollViewer.CanContentScroll" Value="True" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ComboBox}">
                        <Grid>
                            <ToggleButton
                                Name="ToggleButton"
                                Grid.Column="2"
                                ClickMode="Press"
                                Content="{TemplateBinding SelectionBoxItem}"
                                Focusable="False"
                                FontSize="18"
                                Foreground="White"
                                IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                IsTabStop="False"
                                Template="{StaticResource ComboBoxToggleButton}" />
                            <ContentPresenter
                                Name="ContentSite"
                                Margin="3,3,23,3"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Center"
                                IsHitTestVisible="False" />
                            <!--  None, Fade, Scroll, Slide  -->
                            <Popup
                                Name="Popup"
                                AllowsTransparency="True"
                                Focusable="False"
                                IsOpen="{TemplateBinding IsDropDownOpen}"
                                Placement="Bottom"
                                PopupAnimation="Scroll">
                                <Grid
                                    Name="DropDown"
                                    MinWidth="200"
                                    MaxHeight="{TemplateBinding MaxDropDownHeight}"
                                    SnapsToDevicePixels="True">
                                    <Border
                                        x:Name="DropDownBorder"
                                        Background="#1f1f1f"
                                        BorderBrush="#167FFC"
                                        BorderThickness="2"
                                        CornerRadius="6" />
                                    <ScrollViewer Margin="4,6,4,6" SnapsToDevicePixels="True">
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained" />
                                    </ScrollViewer>
                                </Grid>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="HasItems" Value="false">
                                <Setter TargetName="DropDownBorder" Property="MinHeight" Value="95" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="false">
                                <Setter Property="Foreground" Value="White" />
                            </Trigger>
                            <Trigger Property="IsGrouping" Value="true">
                                <Setter Property="ScrollViewer.CanContentScroll" Value="false" />
                            </Trigger>
                            <Trigger SourceName="Popup" Property="Popup.AllowsTransparency" Value="true">
                                <Setter TargetName="DropDownBorder" Property="CornerRadius" Value="0" />
                                <Setter TargetName="DropDownBorder" Property="Margin" Value="0,2,0,0" />
                            </Trigger>
                            <Trigger Property="IsEditable" Value="true">
                                <Setter Property="IsTabStop" Value="false" />
                                <Setter TargetName="ContentSite" Property="Visibility" Value="Hidden" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers />
        </Style>

        <!--  SimpleStyles: ComboBoxItem  -->
        <Style x:Key="{x:Type ComboBoxItem}" TargetType="{x:Type ComboBoxItem}">
            <Setter Property="SnapsToDevicePixels" Value="true" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="FontSize" Value="16" />
            <Setter Property="FontWeight" Value="DemiBold" />
            <Setter Property="OverridesDefaultStyle" Value="true" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ComboBoxItem}">
                        <Border Name="Border" SnapsToDevicePixels="true">
                            <ContentPresenter Margin="3" HorizontalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="true">
                                <Setter TargetName="Border" Property="Background" Value="#167FFC" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="false">
                                <Setter Property="Foreground" Value="White" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="{x:Type TextBox}">
            <Setter Property="CaretBrush" Value="#167FFC" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="FontSize" Value="18" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TextBox}">
                        <Border
                            x:Name="border"
                            Background="#171717"
                            BorderBrush="#167FFC"
                            BorderThickness="1.5"
                            CornerRadius="6">
                            <ScrollViewer
                                x:Name="PART_ContentHost"
                                Margin="10,0,0,0"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#167FFC" />
                            </Trigger>
                            <Trigger Property="IsFocused" Value="False">
                                <Setter TargetName="border" Property="BorderBrush" Value="#121212" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="LogoButton" TargetType="{x:Type Button}">
            <Setter Property="Foreground" Value="White" />
            <Setter Property="FontSize" Value="24" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="border" BorderThickness="3">
                            <Border.Background>
                                <SolidColorBrush Opacity="0.1" Color="#131313" />
                            </Border.Background>
                            <Border.BorderBrush>
                                <SolidColorBrush Opacity="0.3" Color="Black" />
                            </Border.BorderBrush>
                            <Grid>
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Name="Buttn" TargetType="{x:Type Button}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ButtonBase}">
                        <Border
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="7">
                            <Grid>
                                <ContentPresenter
                                    Margin="-2,0,0,0"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center" />
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <ScrollViewer>
            <Grid Height="450" ShowGridLines="True">
                <Grid.Background>
                    <SolidColorBrush Opacity="0.60" Color="#171717" />
                </Grid.Background>
                <!--  Name  -->
                <TextBlock
                    Width="244"
                    Height="30"
                    Margin="26,24,0,0"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Top"
                    Text="Название"
                    TextAlignment="Left" />

                <Grid
                    Margin="30,60,0,0"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Top">
                    <TextBox
                        x:Name="InstanceNameTB"
                        Width="244"
                        Height="40"
                        Text="{Binding Model.Name, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" />

                    <TextBlock
                        Margin="10"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Center"
                        IsHitTestVisible="False"
                        Opacity="0.8"
                        Text="Название сборки">
                        <TextBlock.Style>
                            <Style TargetType="{x:Type TextBlock}">
                                <Setter Property="Visibility" Value="Collapsed" />
                                <Setter Property="Foreground" Value="White" />
                                <Setter Property="FontSize" Value="18" />
                                <Style.Triggers>
                                    <MultiDataTrigger>
                                        <MultiDataTrigger.Conditions>
                                            <Condition Binding="{Binding Text, ElementName=InstanceNameTB}" Value="" />
                                            <Condition Binding="{Binding IsFocused, ElementName=InstanceNameTB}" Value="false" />
                                        </MultiDataTrigger.Conditions>
                                        <MultiDataTrigger.Setters>
                                            <Setter Property="Visibility" Value="Visible" />
                                        </MultiDataTrigger.Setters>
                                    </MultiDataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </Grid>
                <!--  TextChanged="InstanceNameTB_TextChanged"  -->

                <!--  Version  -->
                <TextBlock
                    Width="244"
                    Height="30"
                    Margin="300,26,0,0"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Top"
                    Text="Версия"
                    TextAlignment="Left" />
                <ComboBox
                    Width="200"
                    Height="40"
                    Margin="304,60,0,0"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Top"
                    ItemsSource="{Binding GameVersions}"
                    SelectedValue="{Binding SelectedVersion, UpdateSourceTrigger=PropertyChanged}" />
                <!--  SelectionChanged="VersionCB_SelectionChanged"  -->

                <!--  Logo  -->
                <!--<Button x:Name="InstanceLogo"
                    Style="{StaticResource LogoButton}"
                    Content="Logo"
                    Width="120"
                    Height="120"
                    VerticalAlignment="Top"
                    Margin="0,20,50,0"
                    HorizontalAlignment="Right"/>-->

                <!--  Modloader  -->
                <TextBlock
                    Margin="26,130,0,0"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Top"
                    Text="Modloader" />

                <RadioButton
                    x:Name="NoneSelected"
                    Height="30"
                    Margin="30,170,0,0"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Top"
                    Command="{Binding SwitchModloaderType}"
                    CommandParameter="{x:Static objects:ModloaderType.None}"
                    Content="Отсутствует"
                    IsChecked="True" />
                <!--  Click="SelectInstanceRadioButton_Click"  -->
                <RadioButton
                    x:Name="ForgeSelected"
                    Height="30"
                    Margin="205,170,0,0"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Top"
                    Command="{Binding SwitchModloaderType}"
                    CommandParameter="{x:Static objects:ModloaderType.Forge}"
                    Content="Forge" />
                <!--  Click="SelectInstanceRadioButton_Click"  -->
                <RadioButton
                    x:Name="FabricSelected"
                    Height="30"
                    Margin="320,170,0,0"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Top"
                    Command="{Binding SwitchModloaderType}"
                    CommandParameter="{x:Static objects:ModloaderType.Fabric}"
                    Content="Fabric" />
                <!--  Click="SelectInstanceRadioButton_Click"  -->

                <!--  More  -->
                <ComboBox
                    Width="180"
                    Height="40"
                    Margin="30,20,0,0"
                    HorizontalAlignment="Left"
                    Background="Red"
                    ItemsSource="{Binding ModloaderVersions}"
                    SelectedValue="{Binding SelectedModloaderVersion, UpdateSourceTrigger=PropertyChanged}"
                    Visibility="{Binding IsModloaderSelected, Converter={StaticResource ConvertBoolToVisibility}}" />

                <Button
                    Width="105"
                    Height="35"
                    Margin="26,250,0,0"
                    HorizontalAlignment="Left"
                    Command="{Binding CreateInstance}"
                    Content="Собрать"
                    FontSize="14"
                    FontWeight="DemiBold"
                    Style="{StaticResource DefaultBrandColorButton}" />
                <!--  Click="CreateInstanceButton_Click"  -->
                <Button
                    Width="105"
                    Height="35"
                    Margin="150,250,0,0"
                    HorizontalAlignment="Left"
                    Background="#232527"
                    BorderThickness="0"
                    Content="Больше"
                    FontSize="14"
                    FontWeight="DemiBold"
                    Foreground="White" />
            </Grid>
        </ScrollViewer>
    </Grid>
</UserControl>