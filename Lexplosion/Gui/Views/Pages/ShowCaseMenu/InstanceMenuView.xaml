<UserControl x:Class="Lexplosion.Gui.Views.Pages.ShowCaseMenu.InstanceMenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid>
            <TabControl Background="#18191b" 
                    BorderThickness="1,0,0,0"
                    BorderBrush="#212326" 
                    TabStripPlacement="Left"
                    ItemsSource="{Binding Tabs}" SelectedValue="{Binding SelectedTab}" SelectedIndex="{Binding TabControlSelectedIndex}">
                <TabControl.Resources>
                    <Style TargetType="{x:Type TabItem}" BasedOn="{StaticResource LeftMenuItem}" />
                </TabControl.Resources>
                <TabControl.ContentTemplate>
                    <!-- this is the body of the TabItem template-->
                    <DataTemplate>
                        <ContentControl
                    Content="{Binding Content}" />
                    </DataTemplate>
                </TabControl.ContentTemplate>
            </TabControl>
        </Grid>
    </Grid>
</UserControl>
