<UserControl
    x:Class="Lexplosion.Gui.Views.Pages.MainMenu.MainMenuView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">

    <Grid>
        <TabControl
            Background="#18191b"
            BorderBrush="#212326"
            BorderThickness="1,0,0,0"
            ItemsSource="{Binding Path=MainMenuVM.Tabs, Source={StaticResource MainVM}}"
            SelectedValue="{Binding Path=MainMenuVM.SelectedTab, Source={StaticResource MainVM}}"
            TabStripPlacement="Left">
            <TabControl.Resources>
                <Style BasedOn="{StaticResource LeftMenuItem}" TargetType="{x:Type TabItem}" />
            </TabControl.Resources>
            <TabControl.ContentTemplate>
                <!--  this is the body of the TabItem template  -->
                <DataTemplate>
                    <ContentControl Content="{Binding Content}" />
                </DataTemplate>
            </TabControl.ContentTemplate>
        </TabControl>
    </Grid>
</UserControl>