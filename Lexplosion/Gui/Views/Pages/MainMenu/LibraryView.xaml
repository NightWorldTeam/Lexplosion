<UserControl x:Class="Lexplosion.Gui.Views.Pages.MainMenu.LibraryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewmodels="clr-namespace:Lexplosion.Gui.ViewModels"
             xmlns:customcontrols="clr-namespace:Lexplosion.Gui.Views.CustomControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Background>
        <ImageBrush ImageSource="pack://application:,,,/Assets/images/background/mainBG.png"/>
    </UserControl.Background>

    <UserControl.Resources>
        <DataTemplate DataType="{x:Type viewmodels:InstanceFormViewModel}">
            <customcontrols:InstanceForm Margin="30,15,30,15"/>
        </DataTemplate>
    </UserControl.Resources>


    <Grid x:Name="MainGrid">
        <Grid.Background>
            <SolidColorBrush Color="#121212" Opacity="0.55"/>
        </Grid.Background>
        
        <ListBox Style="{StaticResource LibraryListBox}" ItemContainerStyle="{StaticResource LibraryListBoxItem}" ScrollViewer.CanContentScroll="False">
            <ListBox.ItemsSource>
                <CompositeCollection>
                    <CollectionContainer Collection="{Binding LibraryInstances.Values, Source={StaticResource MainModel}}"/>
                    <ListBoxItem Style="{StaticResource LibraryListBoxItem}" 
                                 Margin="30,15,30,0" 
                                 HorizontalContentAlignment="Center">
                        <Button Style="{StaticResource LibraryAddBtn}"
                                Command="{Binding MainMenuVM.NavigationFactoryCommand, Source={StaticResource MainVM}}"
                                Content="Добавить сборку" Width="620" Height="120"/>
                    </ListBoxItem>
                </CompositeCollection>
            </ListBox.ItemsSource>
        </ListBox>
    </Grid>
</UserControl>