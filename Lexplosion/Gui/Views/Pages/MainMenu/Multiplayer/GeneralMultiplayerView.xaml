<UserControl x:Class="Lexplosion.Gui.Views.Pages.MainMenu.Multiplayer.GeneralMultiplayerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Lexplosion.Gui.Views.Pages.MainMenu.Multiplayer"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <Style TargetType="{x:Type RadioButton}">
            <Setter Property="KeyboardNavigation.IsTabStop" Value="False"/>
            <Setter Property="SnapsToDevicePixels" Value="False" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Width" Value="Auto" />
            <Setter Property="Height" Value="Auto" />
            <Setter Property="FontSize" Value="22" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border>
                            <Grid>
                                <Ellipse x:Name="RadioButton"
                                    Width="20"
                                    Height="20"
                                    StrokeThickness="1"
                                    Stroke="#131313"
                                    Fill="#505050"
                                    HorizontalAlignment="Left"
                                    Margin="2,0,0,0"
                                         SnapsToDevicePixels="False"/>
                                <ContentPresenter Margin="35,-2,0,0"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="RadioButton" Property="Stroke" Value="#167FFC"/>
                                <Setter TargetName="RadioButton" Property="StrokeThickness" Value="6"/>
                                <Setter TargetName="RadioButton" Property="Fill" Value="#EDEDED"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    
    <StackPanel>
        <StackPanel Margin="28,20,0,0">
            <TextBlock Text="Статус" FontSize="16" FontWeight="Medium" Foreground="White"/>
            <TextBlock Text="Нет открытого мира" FontSize="16" FontWeight="Medium" Foreground="White" Opacity="0.8"/>
        </StackPanel>

        <StackPanel Margin="28,40,0,0">
            <TextBlock Text="Приоритетный метод подключения" FontSize="18" FontWeight="Medium" Foreground="White"/>
            <StackPanel Orientation="Horizontal">
                <RadioButton GroupName="ConnectType" Content="Прямое" Margin="0,5,20,5" IsChecked="True"/>
                <RadioButton GroupName="ConnectType" Content="Через ретранслятор" Margin="20,5,0,5"/>
            </StackPanel>
        </StackPanel>
    </StackPanel>
</UserControl>
