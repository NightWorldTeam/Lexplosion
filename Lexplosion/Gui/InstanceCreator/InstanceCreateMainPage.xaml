<Page x:Class="Lexplosion.Gui.InstanceCreator.InstanceCreateMainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Lexplosion.Gui.InstanceCreator"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="700"
      Title="InstanceCreateMainPage">

    <Page.Resources>
        <!-- TextBlock -->
        <Style TargetType="{x:Type TextBlock}">
            <Setter Property="TextAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="Foreground" Value="WhiteSmoke" />
            <Setter Property="Padding" Value="5,0,0,0"/>
        </Style>
        <!-- Flat ComboBox -->
        <ControlTemplate x:Key="ComboBoxToggleButton" TargetType="{x:Type ToggleButton}">
            <Border x:Name="border"
                    CornerRadius="6" 
                    Width="{TemplateBinding Width}" 
                    Height="{TemplateBinding Height}" 
                    Background="#171717"
                    BorderBrush="#121212"
                    BorderThickness="1.5">
                <Grid VerticalAlignment="Center" HorizontalAlignment="Left" Margin="10,0,0,0">
                    <ContentPresenter />
                </Grid>
            </Border>
            <ControlTemplate.Triggers>
                <Trigger Property="IsChecked" Value="True">
                    <Setter TargetName="border" Property="BorderBrush" Value="#167FFC"/>
                    <Setter TargetName="border" Property="BorderThickness" Value="2"/>
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>
        <ControlTemplate x:Key="ComboBoxCheckBox" TargetType="{x:Type CheckBox}">
            <Grid VerticalAlignment="Center" HorizontalAlignment="Left" Margin="10,0,0,0">
                <ContentPresenter />
            </Grid>
        </ControlTemplate>

        <Style x:Key="{x:Type ComboBox}" TargetType="{x:Type ComboBox}">
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto"/>
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="ScrollViewer.CanContentScroll" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ComboBox}">
                        <Grid>
                            <ToggleButton 
                                Name="ToggleButton" 
                                Template="{StaticResource ComboBoxToggleButton}" 
                                Grid.Column="2"
                                IsTabStop="False"
                                Focusable="False"
                                Content="{TemplateBinding SelectionBoxItem}"
                                Foreground="White"
                                FontSize="18"
                                IsChecked="{Binding Path=IsDropDownOpen,Mode=TwoWay,RelativeSource={RelativeSource TemplatedParent}}"
                                ClickMode="Press">
                            </ToggleButton>
                            <ContentPresenter Name="ContentSite" 
                                IsHitTestVisible="False"
                                Margin="3,3,23,3"
                                VerticalAlignment="Center"
                                HorizontalAlignment="Left" />
                            <!-- None, Fade, Scroll, Slide -->
                            <Popup Name="Popup"
                                Placement="Bottom"
                                IsOpen="{TemplateBinding IsDropDownOpen}"
                                AllowsTransparency="True" 
                                Focusable="False"
                                PopupAnimation="Scroll">
                                <Grid Name="DropDown"
                                    SnapsToDevicePixels="True"        
                                    MinWidth="200"
                                    MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border x:Name="DropDownBorder"
                                        CornerRadius="6"
                                        Background="#1f1f1f"
                                        BorderThickness="2"
                                        BorderBrush="#167FFC"/>
                                    <ScrollViewer Margin="4,6,4,6" SnapsToDevicePixels="True">
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained" />
                                    </ScrollViewer>
                                </Grid>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="HasItems" Value="false">
                                <Setter TargetName="DropDownBorder" Property="MinHeight" Value="95"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="false">
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsGrouping" Value="true">
                                <Setter Property="ScrollViewer.CanContentScroll" Value="false"/>
                            </Trigger>
                            <Trigger SourceName="Popup" Property="Popup.AllowsTransparency" Value="true">
                                <Setter TargetName="DropDownBorder" Property="CornerRadius" Value="0"/>
                                <Setter TargetName="DropDownBorder" Property="Margin" Value="0,2,0,0"/>
                            </Trigger>
                            <Trigger Property="IsEditable"  Value="true">
                                <Setter Property="IsTabStop" Value="false"/>
                                <Setter TargetName="ContentSite" Property="Visibility" Value="Hidden"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
            </Style.Triggers>
        </Style>

        <!-- SimpleStyles: ComboBoxItem -->
        <Style x:Key="{x:Type ComboBoxItem}" TargetType="{x:Type ComboBoxItem}">
            <Setter Property="SnapsToDevicePixels" Value="true"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="16" />
            <Setter Property="FontWeight" Value="DemiBold"/>
            <Setter Property="OverridesDefaultStyle" Value="true"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ComboBoxItem}">
                        <Border Name="Border"
                              SnapsToDevicePixels="true">
                            <ContentPresenter HorizontalAlignment="Center" Margin="3" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="true">
                                <Setter TargetName="Border" Property="Background" Value="#167FFC"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="false">
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="{x:Type RadioButton}">
            <Setter Property="KeyboardNavigation.IsTabStop" Value="False"/>
            <Setter Property="SnapsToDevicePixels" Value="False" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Width" Value="Auto" />
            <Setter Property="Height" Value="Auto" />
            <Setter Property="FontSize" Value="22" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border>
                            <Grid>
                                <Ellipse x:Name="RadioButton"
                                    Width="20"
                                    Height="20"
                                    StrokeThickness="1"
                                    Stroke="#131313"
                                    Fill="#505050"
                                    HorizontalAlignment="Left"
                                    Margin="2,0,0,0"
                                         SnapsToDevicePixels="False"/>
                                <ContentPresenter Margin="35,-2,0,0"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="RadioButton" Property="Stroke" Value="#167FFC"/>
                                <Setter TargetName="RadioButton" Property="StrokeThickness" Value="6"/>
                                <Setter TargetName="RadioButton" Property="Fill" Value="#EDEDED"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="{x:Type TextBox}">
            <Setter Property="CaretBrush" Value="#167FFC"/>
            <Setter Property="Foreground" Value="White" />
            <Setter Property="FontSize" Value="18" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TextBox}">
                        <Border x:Name="border" Background="#171717"
                                BorderBrush="#121212"
                                BorderThickness="1.5"
                                CornerRadius="6">
                            <ScrollViewer Margin="10,0,0,0" x:Name="PART_ContentHost" VerticalAlignment="Center" HorizontalAlignment="Left" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#167FFC"/>
                            </Trigger>
                            <Trigger Property="IsFocused" Value="False">
                                <Setter TargetName="border" Property="BorderBrush" Value="#121212"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="LogoButton" TargetType="{x:Type Button}">
            <Setter Property="Foreground" Value="White" />
            <Setter Property="FontSize" Value="24" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="border" BorderThickness="3">
                            <Border.Background>
                                <SolidColorBrush Color="#131313" Opacity="0.1" />
                            </Border.Background>
                            <Border.BorderBrush>
                                <SolidColorBrush Color="Black" Opacity="0.3" />
                            </Border.BorderBrush>
                            <Grid>
                                <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Center"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers></ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Name="Buttn" TargetType="{x:Type Button}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ButtonBase}">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="7">
                            <Grid>
                                <ContentPresenter Margin="-2,0,0,0" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid>
        <ScrollViewer>
            <Grid ShowGridLines="False">
                <Grid.Background>
                    <SolidColorBrush Color="#171717" Opacity="0.60"/>
                </Grid.Background>
                <!-- Name -->
                <TextBlock Text="Название" 
                    Margin="26,24,0,0"
                    Width="244" 
                    Height="30"
                    VerticalAlignment="Top"
                    HorizontalAlignment="Left"
                    TextAlignment="Left"/>
                <TextBox x:Name="InstanceNameTB"
                    Width="244"
                    Height="40"
                    Margin="30,60,0,0"
                    VerticalAlignment="Top"
                    HorizontalAlignment="Left"/>

                <!-- Version -->
                <TextBlock Text="Версия"
                    Margin="300,26,0,0"
                    Width="244" 
                    Height="30"
                    VerticalAlignment="Top"
                    HorizontalAlignment="Left"
                    TextAlignment="Left"/>
                <ComboBox x:Name="VersionCB"
                    Width="180"
                    Height="40"
                    Margin="304,60,0,0"
                    VerticalAlignment="Top"
                    HorizontalAlignment="Left"/>

                <!-- Logo -->
                <Button x:Name="InstanceLogo"
                    Style="{StaticResource LogoButton}"
                    Content="Logo"
                    Width="120"
                    Height="120"
                    VerticalAlignment="Top"
                    Margin="0,20,50,0"
                    HorizontalAlignment="Right"/>
                
                <!-- Modloader -->
                <TextBlock Text="Modloader"
                    Margin="26,130,0,0"
                    VerticalAlignment="Top"
                    HorizontalAlignment="Left"/>
                
                <RadioButton x:Name="NoneSelected"
                    Content="Отсутствует"
                    Height="30"
                    VerticalAlignment="Top"
                    HorizontalAlignment="Left"
                    Margin="30,170,0,0"/>                
                <RadioButton x:Name="ForgeSelected"
                    Content="Forge" 
                    Height="30"
                    VerticalAlignment="Top"
                    HorizontalAlignment="Left"
                    Margin="205,170,0,0"/>                
                <RadioButton x:Name="FabricSelected"
                    Content="Fabric" 
                    Height="30"
                    VerticalAlignment="Top"
                    HorizontalAlignment="Left"
                    Margin="320,170,0,0"/>

                <!-- More -->
                <ComboBox x:Name="ModloaderVersion"
                    Width="180"
                    Height="40"
                    HorizontalAlignment="Left"
                    Margin="30,20,0,0"/>

                <Button x:Name="CreateInstanceButton"
                        BorderThickness="0"
                        Background="#167FFC"
                        Foreground="White"
                        FontSize="14"
                        FontWeight="DemiBold"
                        Content="Создать"
                        HorizontalAlignment="Left"
                        Margin="26,360,0,0"
                        Width="105"
                        Height="35"
                        Click="CreateInstanceButton_Click"/> 
                <Button x:Name="MoreInstanceSettings"
                        Background="#232527" 
                        BorderThickness="0" 
                        Foreground="White"
                        FontSize="14"
                        FontWeight="DemiBold"
                        Content="Больше"
                        Width="105"
                        Height="35"
                        HorizontalAlignment="Left"
                        Margin="150,360,0,0"/> 
            </Grid>
        </ScrollViewer>
    </Grid>
</Page>
