<UserControl
    x:Class="Lexplosion.UI.WPF.Mvvm.Views.Pages.Profile.ProfileCosmeticsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:WpfToolkit.Controls;assembly=VirtualizingWrapPanel"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:extensions="clr-namespace:Lexplosion.UI.WPF.Extensions"
    xmlns:local="clr-namespace:Lexplosion.UI.WPF.Mvvm.Views.Pages.Profile"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:profile="clr-namespace:Lexplosion.UI.WPF.Mvvm.ViewModels.Profile"
    xmlns:sys="clr-namespace:System;assembly=mscorlib"
    d:DataContext="{d:DesignInstance Type=profile:ProfileCosmeticsViewModel}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">

    <UserControl.Resources>
        <sys:String x:Key="Cosmetics">Косметика</sys:String>
        <sys:String x:Key="Head">Голова</sys:String>
        <sys:String x:Key="Body">Тело</sys:String>

        <CollectionViewSource
            x:Key="FriendsViewSource"
            x:Name="FriendsCollectionViewSource"
            Source="{Binding Path=Model.Cosmetics}">
            <CollectionViewSource.GroupDescriptions>
                <PropertyGroupDescription PropertyName="GroupNameKey" />
            </CollectionViewSource.GroupDescriptions>
        </CollectionViewSource>
    </UserControl.Resources>

    <Grid Margin="0,0,6,0" ShowGridLines="False">
        <!--    -->

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.50*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Border
            Padding="16,12,16,0"
            Background="{DynamicResource PrimarySolidColorBrush}"
            BorderBrush="{DynamicResource SeparateSolidColorBrush}"
            BorderThickness="1"
            CornerRadius="5">

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <TextBlock
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    FontSize="18"
                    Foreground="{DynamicResource ActivitySolidColorBrush}"
                    Text="{DynamicResource Cosmetics}" />

                <ComboBox Grid.Row="1" Margin="0,16,0,0" />

                <ListBox
                    Grid.Row="2"
                    Margin="-12,16,-16,0"
                    ItemsSource="{Binding Source={StaticResource FriendsViewSource}}"
                    Style="{StaticResource TransparentListBox}"
                    VirtualizingPanel.IsVirtualizingWhenGrouping="True"
                    VirtualizingPanel.ScrollUnit="Pixel">

                    <ListBox.GroupStyle>
                        <GroupStyle>
                            <GroupStyle.HeaderTemplate>
                                <DataTemplate>
                                    <Border
                                        Margin="16,8,0,8" Padding="0,0,0,8"
                                        BorderBrush="{DynamicResource SeparateSolidColorBrush}"
                                        BorderThickness="0,0,0,1">
                                        <!--  extensions:TextBlockExtensions.TextByKey="{Binding Name}"  -->
                                        <TextBlock
                                            FontSize="16"
                                            FontWeight="Medium"
                                            Foreground="{DynamicResource PrimaryForegroundSolidColorBrush}"
                                            Text="{Binding Name}" />
                                    </Border>
                                </DataTemplate>
                            </GroupStyle.HeaderTemplate>
                        </GroupStyle>
                    </ListBox.GroupStyle>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border
                                Width="120"
                                Height="120"
                                Margin="0,2,0,6"
                                Padding="8"
                                BorderBrush="{DynamicResource SeparateSolidColorBrush}"
                                BorderThickness="1"
                                CornerRadius="10">
                                <Image
                                    FlowDirection="LeftToRight"
                                    Source="{Binding CoverUrl}"
                                    Stretch="Uniform"
                                    StretchDirection="DownOnly" />
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                    <ListBox.ItemsPanel>
                        <ItemsPanelTemplate>
                            <controls:VirtualizingWrapPanel IsItemsHost="True" />
                        </ItemsPanelTemplate>
                    </ListBox.ItemsPanel>
                </ListBox>
            </Grid>

            <!--    -->
        </Border>
        <Border
            Grid.Column="1"
            Margin="8,0,0,0"
            Background="{DynamicResource PrimarySolidColorBrush}"
            BorderBrush="{DynamicResource SeparateSolidColorBrush}"
            BorderThickness="1"
            CornerRadius="5" />
    </Grid>
</UserControl>
