<UserControl
    x:Class="Lexplosion.UI.WPF.Mvvm.Views.Pages.Profile.ProfileLayoutView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:Lexplosion.UI.WPF.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:extensions="clr-namespace:Lexplosion.UI.WPF.Extensions"
    xmlns:local="clr-namespace:Lexplosion.UI.WPF.Mvvm.Views.Pages.Profile"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:objects="clr-namespace:Lexplosion.UI.WPF.Core.Objects"
    xmlns:profile="clr-namespace:Lexplosion.UI.WPF.Mvvm.ViewModels.Profile"
    d:DataContext="{d:DesignInstance Type=profile:ProfileLayoutViewModel}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">

    <UserControl.Resources>
        <DataTemplate DataType="{x:Type objects:TabItemModel}">
            <RadioButton GroupName="ProfileLeftPanel" IsChecked="{Binding IsSelected}">
                <RadioButton.Template>
                    <ControlTemplate TargetType="{x:Type RadioButton}">

                        <Border
                            Width="32"
                            Height="48"
                            Background="Transparent">

                            <Viewbox
                                Width="21"
                                Height="21"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center">
                                <Path
                                    x:Name="Path"
                                    extensions:PathExtensions.StringKeyData="{Binding TextKey}"
                                    Fill="{DynamicResource SecondaryForegroundSolidColorBrush}"
                                    Stretch="Fill" />
                            </Viewbox>
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Path" Property="Fill" Value="{DynamicResource ActivitySolidColorBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </RadioButton.Template>
            </RadioButton>
        </DataTemplate>
    </UserControl.Resources>

    <Grid Margin="8,38,0,0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Border
            Width="42"
            Margin="0,0,8,8"
            Background="{DynamicResource PrimarySolidColorBrush}"
            BorderBrush="{DynamicResource SeparateSolidColorBrush}"
            BorderThickness="1"
            CornerRadius="5">
            <ItemsControl
                Margin="0,8,0,0"
                HorizontalAlignment="Stretch"
                ItemsSource="{Binding Tabs}" />
        </Border>

        <controls:AnimatedContentControl
            Grid.Column="1"
            Margin="0,0,0,8"
            AnimationType="ZoomInEntrance"
            Content="{Binding Content}" />
    </Grid>
</UserControl>
