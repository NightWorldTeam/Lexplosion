<UserControl
    x:Class="Lexplosion.UI.WPF.Mvvm.Views.Pages.AddonsRepositories.Lexplosion.InstalledAddonsRepositoryView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:Lexplosion.UI.WPF.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:instanceprofile="clr-namespace:Lexplosion.UI.WPF.Mvvm.Views.Pages.MainContent.InstanceProfile"
    xmlns:local="clr-namespace:Lexplosion.UI.WPF.Mvvm.Views.Pages.AddonsRepositories.Lexplosion"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid>
        <!--<Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid>
            <controls:SearchBox />
        </Grid>

        <ItemsControl
            Grid.Row="1"
            Margin="0,16,0,0"
            ItemsSource="{Binding Model.InstalledAddons}"
            ScrollViewer.CanContentScroll="True"
            VirtualizingPanel.IsVirtualizing="True" />

        <controls:Empty Grid.Row="1" />-->
        <instanceprofile:InstanceProfileAddonsView DataContext="{Binding}" />
    </Grid>
</UserControl>
