<Window
    x:Class="Lexplosion.WPF.NewInterface.Mvvm.Views.Windows.ConsoleWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:Lexplosion.WPF.NewInterface.Controls"
    xmlns:core="clr-namespace:Lexplosion.WPF.NewInterface.Core"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Lexplosion.WPF.NewInterface.Mvvm.Views.Windows"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="ConsoleWindow"
    Width="800"
    Height="450"
    mc:Ignorable="d">

    <Window.Resources>
        <DataTemplate DataType="{x:Type core:ConsoleLog}">
            <Grid Margin="0,2">

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Border
                    x:Name="Type"
                    Padding="8,4"
                    CornerRadius="5">
                    <TextBlock
                        x:Name="TypeName"
                        FontSize="14"
                        Text="{Binding Type}" />
                </Border>

                <TextBlock
                    Grid.Column="1"
                    Margin="8,0,0,0"
                    VerticalAlignment="Center"
                    FontSize="14"
                    Foreground="Purple"
                    Text="{Binding Time}" />

                <TextBlock
                    x:Name="TextBlock"
                    Grid.Column="2"
                    Margin="8,0,0,0"
                    VerticalAlignment="Center"
                    FontSize="14"
                    Text="{Binding Message}"
                    TextWrapping="WrapWithOverflow" />
            </Grid>

            <DataTemplate.Triggers>

                <DataTrigger Binding="{Binding Type}" Value="{x:Static core:LogType.Info}">
                    <Setter TargetName="TextBlock" Property="Foreground" Value="{DynamicResource PrimaryForegroundSolidColorBrush}" />
                    <Setter TargetName="Type" Property="Background" Value="LightGray" />
                    <Setter TargetName="TypeName" Property="Foreground" Value="Black" />
                    <!--<Setter TargetName="TextBlock" Property="Background">
                        <Setter.Value>
                            <SolidColorBrush Opacity="0.3" Color="#e59f38" />
                        </Setter.Value>
                    </Setter>-->
                </DataTrigger>

                <DataTrigger Binding="{Binding Type}" Value="{x:Static core:LogType.InfoGame}">
                    <Setter TargetName="TextBlock" Property="Foreground" Value="#007BFF" />
                    <Setter TargetName="Type" Property="Background" Value="#007BFF" />
                    <Setter TargetName="TypeName" Property="Foreground" Value="White" />
                    <Setter TargetName="TypeName" Property="Text" Value="Game Info" />
                    <!--<Setter TargetName="TextBlock" Property="Background">
                        <Setter.Value>
                            <SolidColorBrush Opacity="0.3" Color="#e59f38" />
                        </Setter.Value>
                    </Setter>-->
                </DataTrigger>
                <DataTrigger Binding="{Binding Type}" Value="{x:Static core:LogType.Warning}">
                    <Setter TargetName="TextBlock" Property="Foreground" Value="#FFC107" />
                    <Setter TargetName="Type" Property="Background" Value="#FFC107" />
                    <Setter TargetName="TypeName" Property="Background" Value="Black" />
                    <!--<Setter TargetName="TextBlock" Property="Background">
                        <Setter.Value>
                            <SolidColorBrush Opacity="0.1" Color="#167FFC" />
                        </Setter.Value>
                    </Setter>-->
                </DataTrigger>
                <DataTrigger Binding="{Binding Type}" Value="{x:Static core:LogType.Error}">
                    <Setter TargetName="TextBlock" Property="Foreground" Value="#DC3545" />
                    <Setter TargetName="Type" Property="Background" Value="#DC3545" />
                    <Setter TargetName="TypeName" Property="Foreground" Value="#fff" />
                    <!--<Setter TargetName="TextBlock" Property="Background">
                        <Setter.Value>
                            <SolidColorBrush Opacity="0.3" Color="#c94b4b" />
                        </Setter.Value>
                    </Setter>-->
                </DataTrigger>

            </DataTemplate.Triggers>

        </DataTemplate>
    </Window.Resources>

    <Border Background="{DynamicResource SecondarySolidColorBrush}">

        <Grid Margin="16,16,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <controls:SearchBox />

            <Border
                Grid.Row="1"
                Margin="0,8,0,0"
                Padding="0,16,0,8"
                BorderBrush="{DynamicResource SeparateSolidColorBrush}"
                BorderThickness="1"
                CornerRadius="10">

                <ItemsControl x:Name="LogsContainer">

                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <VirtualizingStackPanel IsVirtualizing="True" VirtualizationMode="Recycling" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>

                    <ItemsControl.Template>
                        <ControlTemplate TargetType="ItemsControl">
                            <Border
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                                <ScrollViewer
                                    Padding="{TemplateBinding Padding}"
                                    CanContentScroll="True"
                                    Focusable="False">
                                    <ItemsPresenter />
                                </ScrollViewer>
                            </Border>
                        </ControlTemplate>
                    </ItemsControl.Template>
                </ItemsControl>
            </Border>

        </Grid>
    </Border>
</Window>
