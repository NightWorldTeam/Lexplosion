<UserControl
    x:Class="Lexplosion.WPF.NewInterface.Views.Pages.MainContent.MainMenu.LibraryView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:Lexplosion.WPF.NewInterface.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:instanceModel="clr-namespace:Lexplosion.WPF.NewInterface.Models.InstanceModel"
    xmlns:local="clr-namespace:Lexplosion.WPF.NewInterface.Views.Pages.MainContent.MainMenu"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.0715*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="0.0715*" />
        </Grid.ColumnDefinitions>

        <ItemsControl
            Grid.Column="1"
            Margin="0,32,0,0"
            ItemsSource="{Binding Model.Instances}"
            KeyboardNavigation.IsTabStop="False"
            KeyboardNavigation.TabNavigation="None">

            <ItemsControl.ItemTemplate>
                <DataTemplate DataType="{x:Type instanceModel:InstanceModelBase}">
                    <controls:InstanceForm Margin="0,0,0,16" InstanceModel="{Binding}" />
                </DataTemplate>
            </ItemsControl.ItemTemplate>

        </ItemsControl>
    </Grid>
</UserControl>
